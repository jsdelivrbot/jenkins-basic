pipeline {
    agent none
    options {
        disableResume()
    }
    stages {
        stage('Build') {
            agent { label 'master' }
            steps {
                sh "[ \"\$(git diff --name-only HEAD~1..HEAD | grep '^example/jenkins/' | wc -l)\" -eq 0 ] && false || true"
                echo "Aborting all running jobs ..."
                script {
                    abortAllPreviousBuildInProgress(currentBuild)
                }
                echo "Building ..."
            }
        }
    }
}